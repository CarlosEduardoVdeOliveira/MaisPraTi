let fruitsList = ['Maçã', "Banana", "Uva", "Abacaxi", "Pera"]
let fruitsListSort = [];

function sortArray(arr) {
  for (let i = 0; i < arr.length; i++) {
    if(arr[i][0] > arr[i+1][0]){
      fruitsListSort.push()
    }    
  }
}

/* 

console.log(arrayTeste)
trocou = true
while (trocou) {
    trocou = false
    for (let index = 0; index < arrayTeste.length - 1; index++) {
        const element = arrayTeste[index];
        let varAtu = arrayTeste[index];
        let varProx = arrayTeste[index + 1];

        if (varAtu > varProx) {
            trocou = true
            arrayTeste[index] = varProx;
            arrayTeste[index + 1] = varAtu;
        }
    }
}
console.log(arrayTeste)
let arrayTeste = ['carro', 'banana', 'amendoa', 'tablet', 'dado']

 */